# Copy to `.env` for local development. Both values default sensibly when unset,
# but keeping them explicit makes the port mapping obvious.

# Base URL of the Orbit API.
VITE_API_URL="http://localhost:4000"

# Realtime endpoint. It lives under the API's `/api` prefix.
VITE_WS_URL="ws://localhost:4000/api/realtime"
